# Build artifacts - regenerate via src/build_*.py, large, not needed to run the app
output/

# Portfolio Builder Game runtime state (leaderboard) - resets per game session, not source data
game_state/

# Local Streamlit secrets (Google Sheets service account key etc.) - never commit real credentials
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local Claude Code settings (machine-specific permission allowlist)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
